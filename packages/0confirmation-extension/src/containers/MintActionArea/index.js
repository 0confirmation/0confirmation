import MintActionArea from "./MintActionArea";
import presenter from "./presenter";
import withPresenter from "../../utilities/withPresenter";

export default withPresenter(presenter)(MintActionArea);
