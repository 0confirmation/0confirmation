{
  "name": "@0confirmation/docs",
  "version": "0.1.0",
  "scripts": {
    "examples": "cross-env-shell 'cd website && docusaurus-examples'",
    "start": "cross-env-shell 'cd website && docusaurus-start'",
    "build": "cross-env-shell 'cd website && docusaurus-build'",
    "publish-gh-pages": "cross-env-shell 'cd website && docusaurus-publish'",
    "write-translations": "cross-env-shell 'cd website && docusaurus-write-translations'",
    "version": "cross-env-shell 'cd website && docusaurus-version'",
    "rename-version": "cross-env-shell 'cd website && docusaurus-rename-version'"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "docusaurus": "^1.14.4"
  },
  "license": "MIT"
}
